#include "iostream"
#include <bits/stdc++.h>
#include "vector"

using namespace std;

int firstUniqChar(string s) {
    return 0;
}

int main() {
    cout << "start\n";
    assert(firstUniqChar("leetcode") == 0);
    assert(firstUniqChar("loveleetcode") == 2);

    printf("success\n\n");
    system("pause");
    return EXIT_SUCCESS;
}