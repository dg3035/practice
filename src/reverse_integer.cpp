#include "iostream"
#include <bits/stdc++.h>
#include "vector"

using namespace std;

int reverse(int x) {
    return 0;
}

int main() {
    cout << "here\n\n";

    assert(reverse(123) == 321);
    assert(reverse(-123) == -321);
    assert(reverse(120) == 21);


    printf("success\n\n");
    system("pause");
    return EXIT_SUCCESS;
}